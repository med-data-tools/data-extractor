import { AbstractInferencer } from './abstract-inferencer';

describe('ParentCriterionQuery', () => {
  it('should create an instance', () => {
    expect(new AbstractInferencer()).toBeTruthy();
  });
});
